export type NavLink = {
  name: string;
  link: string;
};

export type BeerItem = {
  title: string;
  imagePath: string;
  beerStyle: string;
  brewery: string;
  description: string;
};
