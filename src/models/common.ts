export type NavLink = {
  name: string;
  link: string;
};

export type BeerItem = {
  name: string;
  imagePath: string;
  beerStyle: string;
  brewery: string;
  description: string;
};
